name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - run: corepack enable

      - run: yarn install --immutable

      - run: yarn build

      - name: Check for broken internal links
        run: npx linkinator ./_site --recurse
